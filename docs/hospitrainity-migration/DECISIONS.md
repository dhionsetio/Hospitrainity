# Hospitrainity Migration Decision Register

| ID | Decision | Status | Consequence |
|---|---|---|---|
| DEC-001 | DOCX is the only learning-material authority. | Confirmed | Every admitted learning entity needs a DOCX chapter/source locator. |
| DEC-002 | Seven equal top-level chapter modules. | Confirmed | Chapter 1 is not hidden pre-learning. |
| DEC-003 | English-only curriculum. | Confirmed | No Indonesian curriculum translation or scaffold. |
| DEC-004 | Grand Serenata Hotel only. | Confirmed | Legacy hotel identity is forbidden in active canonical/generated learning content. |
| DEC-005 | Rewrite legacy material only when chapter-aligned. | Confirmed | Remove unsupported details and document fit. |
| DEC-006 | Archive old progress; reset new progress to zero. | Confirmed | No old-to-new completion/mastery mapping. |
| DEC-007 | Primary audience: pre-service D4 BIKBP and Tourism students at Politeknik Negeri Malang. | Evidence-supported | In-service staff/general public are out of scope. |
| DEC-008 | Mobile-friendly delivery is required. | Evidence-supported | Some learners lack laptops. |
| DEC-009 | Phase 1 cannot publish a final CEFR level. | Guardrail | A2–B1 is a planning hypothesis pending Phase 2. |
| DEC-010 | Use a profiled A2–B1 outcome map, not one global course label. | Draft pending qualified review | Module tasks span routine to less-routine interaction; no public claim before review and diagnostics. |
| DEC-011 | Ratify existing CP-02 codes (COMP-*, OUT-*, CEFR-*) as canonical; use HSP- grammar for new families from CP-04. | Confirmed (CP-03) | CP-02 files stay byte-identical; no re-approval churn. |
| DEC-012 | Mint a fixed project UUIDv5 namespace from the RFC 4122 URL namespace + "https://hospitrainity.curriculum/id". | Confirmed (CP-03) | Deterministic, stable entity UUIDs (cc9dd546-ebaf-51c7-b86f-307d16a22f42). |
| DEC-013 | Canonical package baseline schema_version 1.0.0 / content_version 0.3.0-draft, versioned independently. | Confirmed (CP-03) | Structure and content evolve on separate SemVer tracks. |
| DEC-014 | Validator runtime is Node + Ajv (JSON Schema Draft 2020-12); no PHP/Composer/network. | Confirmed (CP-03) | Runs in the sandbox and CI without a PHP toolchain. |
| DEC-015 | Legacy modules with no DOCX chapter (Pre-Learning, Hotel Amenities, Check-Out, Emergency Situations) are dropped or deferred, not authored. | Confirmed (CP-04) | DOCX is sole source (DEC-001); 3 modules rewritten into chapters, 3 dropped, 1 deferred. |
| DEC-016 | Decompose each chapter into canonical lesson-sections mirroring the manuscript's own step structure. | Confirmed (CP-04) | Deterministic, reproducible; 7 chapters + 85 lesson-sections. |
| DEC-017 | Ship docx-digest.json (normalized per-entity SHA-256) so provenance is verifiable offline without distributing the DOCX. | Confirmed (CP-04) | Validator re-checks every entity hash against the digest. |
| DEC-018 | Chapter 1 (Welcome and Introduction to Customer Care) is an orientation chapter with no Step 5-8 material, so it carries no assessment entities. | Confirmed (CP-05) | Assessment coverage spans chapters 2-7 only; not a gap. |
| DEC-019 | Confidence-check activities (self-rating, no correct answer) inherit their chapter's primary CEFR mode as the reflection target and use scoring_mode self_report. | Confirmed (CP-05) | Keeps self-report reflection inside the CEFR model without inventing a scored outcome. |
| DEC-020 | Role-play rubric criteria (Warmth, Solution, Politeness, Progression; 3 levels) are derived from the manuscript's own explicit evaluative feedback language; wording is provisional pending SME review. | Draft pending qualified review | No new pedagogical claims invented; criteria trace to source feedback, levels ratified in CP-06. |
| DEC-021 | Editorial corrections are provenance-safe: CP-06 never mutates hash-locked verbatim content. Approved changes flow back through Hospitrainity.docx + re-extraction. | Confirmed (CP-06) | Keeps docx-digest/assessment-digest verifiable; CP-06 ships findings + recommendations, not content edits. |
| DEC-022 | Channel-appropriate emoji in Chapter 6 (social-media customer service) content are accepted as-is (verbatim, faithful to source), not flagged as editorial defects. | Confirmed (CP-06) | The 4 editorial-scan info items require no change; SME may re-confirm at ratification. |
| DEC-023 | The renderer/authoring platform targets WCAG 2.2 Level AA + CAST UDL 3.0. Every activity carries a baseline of text_alt + keyboard_path + non_color_cue + no_timing_dependency. | Confirmed (CP-07) | Binds the accessibility vocabulary to specific WCAG success criteria; verifiable via hsp-a11y.mjs. |
| DEC-024 | A transcript / time-based-media alternative is required only when an activity ships pre-recorded audio/video (channel spoken_ftf|telecommunications AND response_form role_play|spoken_rehearsal). | Confirmed (CP-07) | Avoids over-declaring transcript on text-only channels; the 4 spoken/telephone role-plays already declare it (24/24 conform). |
| DEC-025 | CP-07 is requirements-only: no media assets authored and no content mutated; media obligations become enforceable at authoring time (asset entities) and renderer audit (CP-10/CP-12). | Confirmed (CP-07) | Keeps provenance intact at 8/8; assets/ stays empty by design. |
| DEC-026 | Storage uses a document-relational hybrid on PostgreSQL 15+: each entity's full canonical JSON in a doc JSONB column plus extracted scalar columns, code-based foreign keys and lifecycle CHECK constraints; a SQLite mirror serves the offline standalone. | Confirmed (CP-08) | Keeps canonical JSON intact and schema-evolvable while enforcing validator guarantees at rest; pure-relational and pure-document rejected. |
| DEC-027 | Storage is a projection of the canonical package, never a second source of truth. The importer mints no ids (trusts + re-derives UUIDv5), is idempotent and transactional, and re-asserts every validator invariant. | Confirmed (CP-08) | Preserves DEC-001 (DOCX authority) and determinism; a package must pass 8/8 before import. |
| DEC-028 | Array references (outcome_codes[]) and polymorphic references (source-provenance.target_code, migration-edge.canonical_code) are stored as JSONB and enforced by the importer + validator (optionally junction views), not single SQL foreign keys. | Confirmed (CP-08) | A scalar SQL FK cannot express array or any-entity targets; integrity is still verified (525 refs, 0 violations). |
| DEC-029 | CP-09 implements only the package→database importer; the DOCX→package compiler stays the CP-04/CP-05 provenance-locked decomposition and is not re-run. | Confirmed (CP-09) | Preserves DEC-001 (DOCX authority) and every content digest; re-deriving content would risk mutation. |
| DEC-030 | The importer targets Node's built-in node:sqlite (offline mirror) and is content-addressed by a canonical row digest plus a stable curriculum.sql load script, not by the .db file bytes. | Confirmed (CP-09) | SQLite page layout is not byte-reproducible; hashing logical content keeps the build deterministic and reviewable. The compiled .db is disposable and not shipped. |
| DEC-031 | The keyless migration_edge entity (no id/code/status) is stored with a content-hash surrogate primary key (_rowkey) and no lifecycle CHECK; per-table constraints are derived from the columns that actually exist. | Confirmed (CP-09) | migration-edge has no id/code/status, so blindly applying PK(id)/UNIQUE(code)/CHECK(status) would fail; the surrogate keeps the import idempotent. |
| DEC-032 | The learner standalone is a deterministic, read-only projection of the CP-09 compiled canonical store; the frozen hand-authored legacy `window.STAYREADY` payload is retired. The renderer authors no content, sorts every collection by a stable key, emits no timestamps/random ids, and produces byte-identical output from identical inputs. | Confirmed (CP-10) | What learners see is exactly what the DOCX compiled to; the standalone can no longer drift from the source of truth. Rendering from the compiled store also proves the CP-08/CP-09 storage projection is sufficient to drive the learner experience. |
| DEC-033 | Open prompts (self-rating, role-play, service-artifact) carry an answer-model record with an empty `accepted[]` and are scored by rubric or self-assessment; the renderer verifies the true invariant (102/102 answer-model + feedback records, 90/102 with model-answer text) rather than assuming every prompt has a model answer. | Confirmed (CP-10) | Prevents a hallucinated "102 model answers" claim; the 12 open prompts (6 self-rating, 4 role-play, 2 service-artifact) trace to DEC-019/DEC-020 and are rendered with a *Guidance & feedback* reveal instead of a model answer. |

| DEC-034 | The unit of authoring approval is the **module** (one of the 7 chapters). CP-11 submits all 428 authored entities (chapter, lesson-section, activity, prompt-item, answer-model, feedback-model, rubric) `draft → review`, but never `approved`/`published`. The CP-02 framework (52 entities, frozen per DEC-011) and source-provenance (7, immutable verification metadata) are excluded and stay `draft`. `approve`/`publish` are refused until three recorded human sign-offs (esp_cefr_specialist, hospitality_practitioner, accessibility_reviewer) exist and the CP-02 gate closes. | Confirmed (CP-11) | Advances content to the point qualified review can begin without fabricating sign-offs (anti-hallucination); framework stays byte-identical; approval remains a human act. |
| DEC-035 | The lifecycle vocabulary has a single source of truth: `phase-03/controlled-vocabularies.json` (`draft, review, approved, published, retired`). Both the validator and the importer derive their legal-state set from it; the importer no longer hard-codes its own list. | Confirmed (CP-11) | The importer previously used a divergent list (`in_review`, `archived`, `superseded`), a latent bug that only surfaced when a status first left `draft` (SQLite `constraint failed`). Deriving from the vocabulary prevents the two tools from drifting. |
| DEC-036 | CP-12 adds one **integrated QA harness** (`phase-03/tools/hsp-qa.mjs`) that composes the whole pipeline into a single deterministic gate: it treats the id-registry as the authoritative entity inventory and proves parity at three layers (registry ↔ compiled store ↔ rendered view-model ↔ painted DOM), then **delegates** to the real validators (structural 8/8, accessibility, editorial) rather than re-implementing them. | Confirmed (CP-12) | One command is the whole quality gate; cross-checking against the registry (not hard-coded numbers) and re-running the actual validators keeps the result trustworthy and reproducible. |
| DEC-037 | Pilot readiness is a **deterministic two-gate decision** (`phase-12/pilot-readiness.json`): internal-pilot criteria (QA green, deterministic build, 8/8, a11y 0 gaps, no banned terms) are separated from public-release criteria (all modules approved + CP-02 gate closed). Public release is **held** while any module is unapproved or the CP-02 gate is open. | Confirmed (CP-12) | Technical QA can be green while release stays HOLD; prevents presenting un-approved content and provisional CEFR bands as final (anti-hallucination). The verdict is derived from the gate + QA results, not asserted by hand. |
| DEC-038 | The CP-02 human-approval gate is recorded **closed**, and all 7 modules' required sign-offs (esp_cefr_specialist, hospitality_practitioner, accessibility_reviewer) are recorded as signed, on the basis of the Project Owner's explicit final release verdict (2026-07-16: "The three per-module signoffs and CP-02 Gate are all approved. Please implement."), recorded in `phase-14/cp02-gate-closure.json` and `phase-14/signoffs.json`. All 428 authored entities across the 7 modules were then moved `review -> approved -> published` via the same guarded workflow engine used since CP-11 (no shortcutting of `hsp-workflow.mjs`'s legal-transition checks). | Recorded (CP-14) | This is explicitly a **Project-Owner consolidated verdict**, not three independently-conducted specialist reviews by distinct qualified reviewers. That basis is disclosed transparently on the About page, in the workflow evidence files, and in this changelog/decision entry, so the provenance of the approval is never misrepresented (anti-hallucination). Provisional CEFR bands (A2\u2013B1) remain working hypotheses and are not certified by this closure. |
| DEC-039 | Following DEC-038's gate closure and publication, the standalone footer and About-page release-policy copy were simplified: the footer no longer states "generated from the compiled canonical store" / "is the source of truth" (pipeline/AI-generation disclosure language), and now shows only the product name, content version, and an About & sourcing link. The About page's release-policy paragraph is now dynamic on gate status, showing the recorded closure basis when closed instead of the CP-12/CP-13 "internal-pilot-only, gate open" copy. | Confirmed (CP-14) | Requested directly by the Project Owner ("Remove unnecessary AI related details such as shown in the web footer"). The full sourcing/provenance detail remains available and undiminished on the About page — nothing about provenance was hidden, only the redundant/informal footer phrasing was removed. |

## Open evidence gates
- Actual learner CEFR entry distribution: diagnostic or validated survey required. Still open.
- Final CEFR alignments: official descriptor register and qualified review required. Still open — provisional A2\u2013B1 bands are not affected by the CP-14 gate closure (DEC-038).
- Final reviewer identities for genuinely independent specialist review: still open — CP-14's sign-offs are a Project-Owner consolidated verdict (DEC-038), not three independently-conducted specialist reviews.
- Module approval sign-offs (CP-11/CP-14): **Resolved for this release.** All 7 modules recorded signed and moved `review -> approved -> published` (see `phase-14/signoffs.json`, `phase-14/workflow-report.json`: `modules_published:7`, `modules_in_review:0`, `human_approval_gate:"closed"`).
- Accessibility and network/device profile: learner evidence required. Still open.
- Public release (CP-12/CP-14): **Resolved.** `phase-14/pilot-readiness.json` verdict is `READY` with `public_release_ready:true`, on the recorded CP-02 gate closure and module publication (DEC-038).
- CP-13 rebrand/legacy-code-removal/rollback plan: unchanged by this closing phase — still outstanding (see CHECKPOINTS.md row 13).
